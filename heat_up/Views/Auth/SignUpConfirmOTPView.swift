//
//  SignUpConfirmOTPView.swift
//  heat_up
//
//  Created by marat orozaliev on 21/12/2024.
//

import SwiftUI

struct SignUpConfirmOTPView: View {
    var body: some View {
        VStack {
            Text("TODO: Implement OTP confirmation logic")
                .font(.largeTitle)
                .fontWeight(.bold)
                .foregroundColor(.blue)
                .padding()
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
        .background(Color(.systemGray6)) // Светлый фон
        .ignoresSafeArea()
    }
}

struct SignUpConfirmOTPViewPreview: PreviewProvider {
    static var previews: some View {
        SignUpConfirmOTPView()
    }
}
